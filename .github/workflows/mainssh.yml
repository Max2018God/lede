#powered by max 3cc 2020 2 12
name: Ubuntu
on:
  watch:
    types: [started]
jobs:
  Ubuntu:
    runs-on: ubuntu-latest
    steps:
    - name: SSH connection to Actions
      uses: p3terx/debugger-action@master
